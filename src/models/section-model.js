class Section {
    constructor({ name, recipe, slug, url }) {
        this.name = name;
        this.recipe = recipe;
        this.slug = slug;
        this.url = url;
    }
}

export default Section;
